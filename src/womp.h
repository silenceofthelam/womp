#ifndef __womp__h
#define __womp__h

#include <stdio.h>
#include <stdlib.h>
#include "./running_config.h"
#include "../lib/word.h"

void permutate_all(char* string, struct running_config* config);

#endif
